# Airflow Providers (use versions compatible with Airflow 2.8.0)
apache-airflow-providers-postgres
apache-airflow-providers-redis
apache-airflow-providers-celery

# Web Scraping (simplified - avoid complex dependency trees)
beautifulsoup4
lxml
requests

# Scrapy and Playwright can be added later if needed
# They have complex dependencies that may conflict with Airflow
# scrapy==2.11.0
# playwright==1.40.0
# selenium==4.16.0

# Data Processing
pandas
numpy

# Database (psycopg2-binary already included in Airflow base image)
# sqlalchemy already included in Airflow base image

# HTTP Clients
httpx

# Utilities
python-dotenv
tenacity
# pydantic already included in Airflow base image

# Development
pytest
pytest-asyncio
